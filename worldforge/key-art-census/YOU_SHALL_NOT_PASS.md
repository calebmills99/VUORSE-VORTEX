# YOU SHALL NOT PASS — Character Drift

*The Gray Eye holds this bridge.*

**Status of all images:** `design` — never auto-canon.

| Rule | Meaning |
|------|---------|
| No lock, no generate | genmedia may not mint a new plate for a character without a designated `design-lock` path (or explicit `gap-first-plate` brief) |
| One face per id | Each character id has at most one active design-lock; other files are `variant` / `rejected` / `orphan` — never competing truths |
| Refs or refuse | Consistency runs must `genmedia upload` the lock and pass it as reference; prompt-only regen of a known face is forbidden |
| Style forks are costumes, not new souls | Pitch-deck Almodóvar / Waters / Romero / Tarantino sets are style variants of the same design lineage, not alternate canons |
| Favorites feed mood, not faces | Steward favorites inform atmosphere; they do not silently rewrite character locks |
| Design ≠ canon | Even a perfect lock stays `design` until the steward says promote |
| Orphans get named or quarantined | Untitled residents = map failure until `linked` or listed in `orphan[]` |

Any genmedia step that violates this gate is a failed step. Redo. Do not shrug.

— Gandalf the Gray Eye

## Addendum — LoRA test dumps

`Slayverse/assets/lora_training/` (including `sassy_1024`) is **out of bounds** for design-locks and genmedia refs unless the steward explicitly promotes a specific file. Messy training tests stay outside the key-art path.
